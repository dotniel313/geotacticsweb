User-agent: *
Disallow: /

# ALPHA — bloqueo global (DEV/STAGING).
# Contradicción conocida: /que-hacemos y /productos emiten meta robots
# index,follow mientras este archivo Disallow: /. El crawler respeta
# robots.txt; NO hay indexación pública mientras este Disallow exista.
#
# PRE-CUTOVER (producción):
#   DEV/STAGING = bloqueado (mantener Disallow o noindex coherente)
#   PRODUCTION  = robots.txt, canonical y meta robots alineados
#                 (Home/indexables según cutover; no dejar Disallow + index)
#
# No cutover en 3.6.0-alpha.1.
#
# Sitemap generado: /sitemap.xml
# En producción (post-cutover): Allow: / y Sitemap: https://www.geotactics.com.ec/sitemap.xml
# Home permanece noindex hasta decisión de cutover.
